from setuptools import setup, find_packages

setup(
    name='PyFs',
    version='0.1.0',
    author='lllrmm',
    description='',
    url='',
    packages=find_packages(),
    install_requires=[
        'pathlib',
    ],
    classifiers=[],
    python_requires='>=3.10',
)